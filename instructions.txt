* python.exe -m pip install --upgrade pip
* Installing Django: pip install Django==3.1.3 // install django version 3.1.3
* pip freeze // django installation verification
* django-admin startproject Universidad // project creation
* mkdir Aplicaciones // a folder is created for the applications
* django-admin startapp Academico // an application of the project is created
* Create model Curso
* Register model Curso in the admin.py archive
* The migration of the model created with the command "python manage.py makemigrations" is applied
* The course model is migrated: python manage.py migrate

